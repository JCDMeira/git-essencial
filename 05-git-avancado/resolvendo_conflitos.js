/*
    _ acontecem conflitos quando o remoto está mais atualizado que o local
    _ atualizar os arquivos locais, com ou sem a indicação de origin
    ? git pull -u oirin master
    ? git pul
    #vai gerar conflito, sendo só apagar o que precisa dentro das marcações

    @ se abrir terminal e não tiver alterações ou conflitos, se usa o gravar -> enter -> sair
*/
