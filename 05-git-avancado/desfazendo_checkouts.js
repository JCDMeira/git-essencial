/*
    _ voltar todos arquivos ao estado original
    ? git checkout -- .
    
    _ voltar um arquivo ao estado original
    ? git checkout -- fine_name

    _ tirando arquivos já adicionados, e voltando para estado anterior
    ? git checkout HEAD -- .
*/
