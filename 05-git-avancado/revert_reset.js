/*
    _ revert irá criar um commit que faz ações contrárias ao que foi feito
    ? git revert hasj_id 
    @ isso mantém fielmente o histórico de tudo que aconteceu
    ! exemplo: ao ter um problema e testar soluções você mantém o histórico dos testes, sabendo o que já tentou

    _ git reset deleta mesmo o commit
    ? git reset HEAD~1 
    # HEAD é simbolico ao topo e ~numero_desejado é quanto commits a partir do topo se deseja deletar
*/
