/*
    _ tags geram uma versão pronta pra uso, mesmo que ela seja incompleta
    ! pode também gerar uma release, uma versão completa, V1 por exemplo

    ? git tag minha_tag

    _ deve ser subido com a tag
    ? git push origin minha_tag

    ! estando na tag o repositório não aceita mais commits
    _ criar um branch a partir da tag
    ? git switch -c nova_branch

    @ Se corrigiria o que fosse necessário na branch de correção, e então faria os commits, megearia na master e criaria a nova versão estável. (uma nova tag)
    @ esse poderia ser V1.1 por exemplo, isso dependendo dos pachs de nomeação que estão sendo usadas.
*/
