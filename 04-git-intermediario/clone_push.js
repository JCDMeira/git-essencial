/*
    _ clone literalmente clona um repositório existente
    ? git clone endereco_repositorio

    _ push envia os commits para o repositório remoto
    ? git push 
    # o primeiro push terá que indicar a origim
*/
