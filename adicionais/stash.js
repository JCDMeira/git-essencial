/*
    _ guarta alterações do working directory (diretório que está trabalhando)
    _ com isso permite fazer rebase, merge ou trocar de branch sem commitar as mudanças

    ? git stash
    # guarda as mudanças atuais em um "cache"

    ? git stash list
    # lista os stash's guardados

    ? git stash poop
    # reaplica as alterações do último stash

    @ muito últil também para pegar alterações do local, criar uma branch ou ir para uma já criada 
    @ e então soltar as alterações ali

    _ stash funciona como uma pilha
*/
