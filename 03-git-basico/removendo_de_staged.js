/*
    _ retirando um arquivo de staged com:
    ? git restore --staged nome_arquivo
    
    _ usar checkout para voltar 
    ? git checkout nome_arquivo
    #volta os que está modificado para o estado do último commit

    _ voltar o estado de vários arquivos com o
    ? git reset HEAD --hard
    #volta todos os que estão modificados para o estado do último commit

    _ Resetar o último commit e descartar as alterações 
    ? git reset HEAD^ --hard
    # volta o estado em um commit, e retira as alterações
*/
