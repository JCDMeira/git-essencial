/*
    _ corrigir uma mensagem errada no commit
    ? git commit --amend -m "mensagem corrigida"
    # amend significa alterar/altera
    - é um novo commit, mas ele adiciona as novas coisas adicionadas também e altera a mensagem

    ? git commit --amend --no-edit
    - adiciona as novas alterações e faz um novo commit, mas não altera a mensagem

*/
