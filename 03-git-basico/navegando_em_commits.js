/*
    _ É possível navegar entre os commits do hisstórico, assim voltando como o projeto estava naquele momento
    _ para isso basta usar o comando de checkout com o hash id do commit.
    ? git checkout hash_id

    _ para para voltar para  master basta usar o checkout com master:
    ? git checkout master


*/
