/*
    _ Fork de um repositório cria uma estrutura igual ao repositório original
    _ mas ele é do seu usuário, e é citado que o repositório foi feito a partir de outro repositório.

    @ Reatribuir uma origin caso presice
    ? git remote -v
    ? git remote set-url origin caminho_rep_remoto
*/
